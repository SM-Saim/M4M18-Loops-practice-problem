// display every number of an array using loop
var numbers = [33, 55, 76, 89, 33, 66, 87, 11, 98];
for (var i = 0; i < numbers.length; i++) {
  var number = numbers[i];
  console.log(number);
}

// Ex:2  display every elements of the array using loop
var tableItems = ["Bottle", "notebook", "lamp", "watch", "mouse"];
for (var i = 0; i < tableItems.length; i++) {
  item = tableItems[i];
  console.log(item);
}
